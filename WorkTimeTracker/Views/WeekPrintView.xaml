<UserControl x:Class="WorkTimeTracker.Views.WeekPrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:WorkTimeTracker.ViewModels">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Tabel -->
            <RowDefinition Height="Auto"/>
            <!-- Footer (totaal overuren) -->
        </Grid.RowDefinitions>

        <!-- HEADER: weektitel -->
        <TextBlock Grid.Row="0"
                   Text="{Binding HeaderText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,8"/>

        <!-- TABEL MET 7 DAGEN (read-only, lijkend op WeekView) -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Dagen, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserResizeColumns="False"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  SelectionMode="Single"
                  SelectionUnit="Cell"
                  IsReadOnly="True"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  Background="{DynamicResource MaterialDesignPaper}"           
                  materialDesign:DataGridAssist.CellPadding="8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="16,8"
                  GridLinesVisibility="Horizontal">

            <!-- Rij-stijl: zoals WeekView, maar zonder 'vandaag'-rand -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="MinHeight" Value="45"/>

                    <Style.Triggers>
                        <!-- Geen highlight -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>

                        <!-- Geen normale werkdag: lichtblauw + vet/cursief (rij-niveau) -->
                        <DataTrigger Binding="{Binding IsNormaleWerkdag}" Value="False">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontStyle"  Value="Italic"/>
                            <Setter Property="Background" Value="#FFF4F6FB"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <!-- Cells: geen focus-/selectie-randjes nodig op print -->
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="IsTabStop" Value="False"/>

                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <!-- Kolommen gebaseerd op jouw WeekView (maar TextBlocks) -->
            <DataGrid.Columns>

                <!-- KOLOM 1: dag + datum (zelfde tekst als DagEnDatum) -->
                <DataGridTextColumn Header="Dag"
                                    Width="Auto"
                                    MinWidth="150"
                                    FontWeight="Bold"
                                    FontSize="18"
                                    Binding="{Binding DagEnDatum}"/>

                <!-- KOLOM 2: Tijd (Start - Eind, alleen bij normale werkdag) -->
                <DataGridTemplateColumn Header="Tijd" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel 
                                x:Name="TijdPanel"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center">
                                <TextBlock Text="{Binding StartTijd, StringFormat='hh\\:mm'}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text=" - " Margin="2,0" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding EindTijd, StringFormat='hh\\:mm'}" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>

                            <DataTemplate.Triggers>
                                <!-- Geen tijd tonen als het geen normale werkdag is -->
                                <DataTrigger Binding="{Binding IsNormaleWerkdag}" Value="False">
                                    <Setter TargetName="TijdPanel" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>

                                <!-- Safety: geen tijd tonen als start of eind ontbreekt -->
                                <DataTrigger Binding="{Binding StartTijd}" Value="{x:Null}">
                                    <Setter TargetName="TijdPanel" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EindTijd}" Value="{x:Null}">
                                    <Setter TargetName="TijdPanel" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- KOLOM 3: Project -->
                <DataGridTemplateColumn Header="Project" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Projectnaam}"
                                       TextTrimming="CharacterEllipsis" 
                                       FontSize="16">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsNormaleWerkdag}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- KOLOM 4: Extra info / Dagtype (zoals op je scherm) -->
                <DataGridTemplateColumn Header="Extra informatie" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <!-- Normale werkdag: extra info -->
                                <TextBlock x:Name="ExtraText"
                                           Text="{Binding ExtraInformatie}"
                                           TextTrimming="CharacterEllipsis" 
                                           FontSize="16"/>

                                <!-- Geen normale werkdag: grote dagstatus over de rij -->
                                <TextBlock x:Name="StatusLabel"
                                           Text="{Binding Status}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="24"
                                           FontWeight="Bold"
                                           FontStyle="Italic"
                                           Opacity="0.8"
                                           Visibility="Collapsed"/>
                            </Grid>

                            <DataTemplate.Triggers>
                                <!-- Normale werkdag -->
                                <DataTrigger Binding="{Binding IsNormaleWerkdag}" Value="True">
                                    <Setter TargetName="ExtraText"   Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="StatusLabel" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>

                                <!-- Niet-normale werkdag -->
                                <DataTrigger Binding="{Binding IsNormaleWerkdag}" Value="False">
                                    <Setter TargetName="ExtraText"   Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="StatusLabel" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- KOLOM 5: Locatie -->
                <DataGridTemplateColumn Header="Locatie" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Locatie}"
                                       TextTrimming="CharacterEllipsis" 
                                       FontSize="16">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsNormaleWerkdag}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <!-- FOOTER: totaal overuren rechtsonder, maar niet tegen de rand -->
        <TextBlock Grid.Row="2"
                   Text="{Binding OverurenTekst, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Right"
                   Margin="0,4,0,0"/>
    </Grid>
</UserControl>
